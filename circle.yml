machine:
  node:
    version: 6
  ruby:
    version: 2.4.0
  timezone:
    Asia/Tokyo
dependencies:
  override:
  - gem install github_changelog_generator
deployment:
  deploy:
    branch: master
    commands:
      - github_changelog_generator -t ${CHANGELOG_GITHUB_TOKEN} ma-bo-do-fu/circleci_test
